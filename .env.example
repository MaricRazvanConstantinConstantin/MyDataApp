### Copy this file to .env.local and fill values. DO NOT commit real keys.
# Vite-exposed vars (browser-safe)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_ADMIN_EMAILS=admin@example.com
VITE_APP_NAME=RecipeManager

# Server-only (do NOT put these in .env.local used by the browser)
# These should be stored in your deployment host secrets / CI env.
# SUPABASE_SERVICE_ROLE_KEY=
